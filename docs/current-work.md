# Current Work Pointer

Mutable task status is intentionally not duplicated in this repository.

Use, in order:

1. Workspace shared state for the current owner and paused handoffs.
2. The current task receipt for scope, evidence and next owner.
3. Live GitHub PR/check state for branch delivery status.
4. Live Vercel read-back for deployment status.

This pointer never authorizes push, merge, deployment, environment mutation or data mutation.

Article optimization workflow, migration evidence, production steps and rollback: [runbook](./admin-layer/article-optimize-workflow.md). Current execution status remains in receipt `sanity-optimize-20260909`.

Recorded milestone (2026-09-09): PR #83 merged as `aea1f3b99bdc9408957b6d1f5dc5a75e2926fa29`; the separately approved Production normalization completed atomically (3 raw documents / 8 rows; 61-record integrity check; zero remaining patches). Web and Admin Production are READY at the same merge SHA; authenticated read-only AIA Vitality Studio visual QA passed (banner, review guard, URL locks and object cards). No article was edited or published during QA. Read the receipt for newer evidence before acting.

Current branch (2026-09-10): `fix/content-data-foundation-20260910`, PR #87, base `v4-production`. Scope completed on branch: lightweight Sanity list/card projection; strict full-article validation retained; invalid list rows fail isolated; related-article failures fail open; related ranking prefers semantic topic/category; `/blog/` owns canonical Open Graph/Twitter metadata; regression guards added. Production content and motor-insurance drafts were not changed. Neon clean-view audit found 383/383 distinct content IDs in the three current clean views, so no database de-duplication migration is warranted. Existing `ccpun_social_runtime` role is limited to SELECT/INSERT; no new runtime role was created. Neon `main` protection remains blocked by protected-branch quota on the current plan.

Visual QA milestone (2026-09-10): authenticated Vercel Web Preview was checked at 390 / 600 / 820 / 1100 / 1440 across Home, Blog, Health category, AIA Health CI Hero, CI Planning and Financial Health Check. The Web Preview is correctly pinned to Sanity UAT; a direct read-only audit confirmed UAT has 0 published articles while Production has 5, so empty Blog/category cards and the representative article 404 on Vercel Preview are expected lane behavior, not a `listArticles()` regression. The same branch was therefore run through the repository's `local:production:read` mode (draft writes disabled) for full published-content visual QA. All 30 route/viewport comparisons had zero layout-regression groups against current Production; Blog OG/Twitter checks passed; related Health CI Hero is Health Happy then AIA Vitality. A pre-existing CI Planning progress label can extend about 12px left at 820px, but current Production behaves the same and it was intentionally not changed in PR #87.

CSS 4.3 cleanup milestone (2026-09-10): only settled FinalPolish rules were consolidated into `Website43.module.css` (Thai line-breaking contract, article source URL wrapping, Blog grid alignment/category trigger, Home hero image crop). `Website43TransitionStyles` and breakpoint architecture were not changed. Targeted computed-style comparison against Production passed 15/15 probes across the five reference widths after consolidation. Local `npm run check:foundation` passed on Node 24.11.1, including lint, TypeScript, architecture/foundation/Vercel/admin regressions (404 tests, 0 failures in the final Node test batch).

Delivery verification (2026-09-10): CSS consolidation commit `6f436cbc6c80646b4b130972cc604bacb193fe1f` was pushed to PR #87. Fresh CCPun Foundation CI run `34389137889` completed SUCCESS, including the Production-parity build; fresh Vercel Web deployment `dpl_7qoGpJPyrCzjJt9SP7E6RFAGtsaV` and Admin deployment `3eZJSivzbJHANNzbxGGgqQZAYezD` are READY/SUCCESS. The refreshed authenticated Web Preview was rechecked at 390 / 600 / 820 / 1100 / 1440: Home, CI Planning and Financial Health Check remained zero-diff against Production at every viewport; Blog/category/article differences are limited to the already-proven UAT published=0 lane. `/blog/` canonical/Open Graph/Twitter metadata still passed. Final source consumer audit found no `listArticles()` consumer that depends on body/FAQ/sources/review/geo, and final diff review remains scoped to eight files with no Sanity Production, Neon, routing, analytics, consent or URL-contract mutation. Next task: re-run live PR/check state after this documentation-only status update, verify the resulting Preview is healthy, then merge PR #87 to `v4-production` only if the checks remain green; immediately verify the Production deployment and read-only Blog/article metadata/runtime/freshness behavior after merge.

Current branch (2026-09-11): `fix/seo-copy-source-of-truth-20260911`, base `v4-production`. Scope is copy-only synchronization from the user-maintained Google Sheet `Thai Copy Review` column `Your Revised Text (Source of Truth)`: homepage title/meta/OG/X copy, global Organization/Person schema wording and credentials, shared Website 4.3 article author role/bio, and Website 4.3 footer role. No calculator logic, routing, Sanity content, analytics, consent, Neon data, or production branch was changed. Vercel Preview for head `0f45ee111a94cf903e9b2235056870d29c660af2` completed READY as deployment `dpl_6RqMcbyowD7NQq4eUmsot3bdxihW`; errors-only build log reported Build Completed with no build errors. Production merge remains explicitly pending user Preview approval.

Scheduling handoff (2026-09-11): PR #96 is the separate article-scheduling workstream; do not merge unrelated website/CI/FHC work into it. Follow [the scheduler runbook](./article-scheduling-free-plan.md) and [the validation/readback receipt](./article-scheduling-validation-20260911.md). Code SHA `e91c428e9b292d869ec9c6c5b696851628660ae4` passed Foundation, scheduler/Postgres/mounted-control tests and privacy; its Admin Preview is READY. Both exact Neon lanes have the additive private queue tables, `enabled=false`, and zero queue/audit rows. UAT retained its existing restricted LOGIN role; the newly created Production scheduler role remains NOLOGIN with no password issued here. No article content was mutated or published. Authenticated owner Preview QA, secure runtime configuration and human review remain required before merge/activation; this pointer does not grant them.

Admin Control Plane repair (2026-09-13): branch `fix/admin-control-plane-production-20260913`, Draft PR #113, base `v4-production`. Root cause: generic `ccpun_admin` operational state was intentionally UAT-only while Production Neon had only scheduler tables; this kept Audit/Reviews/SEO persistence fail-closed in `production-admin`. The branch adds an exact Production lane, additive checksum-locked operational schema that preserves Article Scheduler grants, owner-only `/snt-admin/health/`, dashboard Control Plane readiness, explicit SEO re-audit UI, corrected Audit source copy, and a digest/lineage-guarded legacy backfill from `kyfxgjnq/production`. Neon migration rehearsal ran only on temporary branch `br-quiet-river-b3t6udt4`; readback passed database/checksum/identity/least-privilege/Social-denial checks and `scheduler_grants_preserved=true`. Legacy source inventory is 19 audit logs, 1 research snapshot and 2 SEO suggestions. Vercel Preview build for initial PR head completed READY and included `/snt-admin/health`; Production DB, runtime role LOGIN/password, Production Vercel environment and `v4-production` remain unchanged. Final Production migration apply is a separate explicit gate after latest CI/Preview pass.

Admin Control Plane route completion (2026-09-14): branch `codex/admin-control-plane-completion-20260914`, base `v4-production` at `1a43900d4bdc6698139094bb616c6ddbbea9d91f`. The active App Router now owns `/login`, `/dashboard`, `/content`, `/seo`, `/social`, `/analytics`, `/operations` and `/settings`; Studio remains `/studio/`, and Admin APIs now live under `/api/admin/*`. Old page GETs use an explicit temporary redirect map. `/api/snt-admin/*` is not canonical runtime: one `beforeFiles` compatibility rewrite forwards it to the same `/api/admin/*` handler without an HTTP redirect, preserving method and body for delayed callers and the existing OAuth callback contract. The Admin host root chooses `/login/` or `/dashboard/`; protected deep links use same-origin validated return paths; unauthenticated APIs return JSON `401`. Public Production denies canonical and legacy Admin pages/APIs, Studio and Preview surfaces. Local verification passed Admin tests (438 total, 437 pass, one real-Postgres integration skipped), ESLint, TypeScript, architecture, Vercel/public regression, foundation contracts and a Webpack Production build (71/71 pages). Native Turbopack build stalled locally during compile; no Production, provider, database, job or environment mutation occurred. A dependency follow-up uses narrow overrides for `smol-toml@1.8.0`, `undici@7.29.0` and `adm-zip@0.6.1`; a clean `npm ci` plus lockfile and installed-tree audits report zero vulnerabilities. Deployment must remain Git-traceable from this branch commit through reviewed PR merge to `v4-production`, then provider read-back must prove the exact deployed SHA. Rollback is deployment rollback or code revert of that exact merge while retaining the compatibility rewrite; do not reset operational data, truncate queues, replay publishers or treat code rollback as cancellation of durable jobs.

Admin Preview UAT correction (2026-09-14): the first Preview of the route-completion commit exposed the public website at the Admin project root because an unset application lane remained `unknown`, the proxy did not treat `admin-uat` as a deployed Admin lane, and generated Vercel aliases were not host-matched at `/`. The source correction infers `admin-uat` only when `CCPUN_APP_ENV` is absent or blank, Vercel supplies `VERCEL_ENV=preview`, and Vercel supplies the immutable Admin project ID. Any explicit valid or invalid application value remains authoritative; Web and unknown projects cannot infer Admin. A separate deny-only boundary makes the exact Admin Vercel project and known Admin hosts return `404` instead of Public content when that authoritative value is missing, invalid or belongs to another lane; neither Host nor Project identity grants Admin access. Root requests always enter the boundary, while generated `ccpun-admin*.vercel.app` and exact `localhost` requests receive catch-all coverage: valid Admin Preview and dedicated local Admin root redirect to `/login/` (or `/dashboard/` for an authenticated owner), Admin routes stay protected, public/unknown routes use the branded Admin `404`, and unauthenticated `/api/admin/*` stays JSON `401`. Production mapping and the exact `AUTH_URL`/local-host contracts are unchanged. The lane remains pinned to Sanity `ccb9lnw5/uat`; Neon operations still require their existing exact UAT identity and restricted runtime connection, with no credential fallback. No provider, Sanity, Neon or Production mutation was performed by this correction; Preview deployment and authenticated browser UAT remain separate gates.

Category Registry delivery track (2026-09-16): branch `fix/blog-category-menu-registry-20260916`, PR #134, now contains the end-to-end physical Blog Category Registry implementation. Sanity Category owns physical category activation (`draft | active`), canonical `/blog/<slug>/`, direct guarded deactivation redirects and cross-record collision safety; semantic `BLOG_TOPIC_HUBS` remains a separate SEO/topic layer. Runtime registry parsing/fetching is fail-soft per category and on Sanity request failure; Blog archive dropdown, physical category pages, canonical metadata and Blog sitemap share that registry. Draft category/article visibility is gated by both Draft Mode and the approved preview environment and remains noindex. Article Studio selects only `status == 'active'` categories and rejects article slug/canonical claims that collide with Category ownership. `motor-insurance` is covered as a physical registry category; deterministic `travel-insurance` lifecycle QA covers Draft → Active → safe Draft redirect. Existing URL/canonical overrides and legacy redirects are retained. Final local Node 24.11.1 `npm run check:foundation` is green; `npm run test:vercel` and `npm run test:admin` are green (Admin final batch 480 pass / 0 fail / 1 skipped). Guarded dry-runs against live UAT and Production Sanity both pass: Production plans only five `status: active` patches on the exact existing five Category documents with identity/title/slug/description unchanged; UAT plans four existing physical categories active, `personal-finance-uat` and `critical-illness` Draft, plus an exact `motor-insurance` seed from Production. No live Category mutation, travel lifecycle mutation, merge or Production deployment has occurred yet at this pointer; next gates are commit/push, fresh PR CI/Preview, explicit UAT apply + QA, then Production data/app cutover only after those gates are green.
