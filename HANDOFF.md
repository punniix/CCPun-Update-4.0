# Repository Handoff Pointer

This file does not store mutable task state or grant approval.

Before repository work:

1. Read the workspace shared state and current task receipt.
2. For PR57 social workspace work, treat [`docs/admin-layer/social-product-contract.md`](./docs/admin-layer/social-product-contract.md) as the acceptance source of truth.
3. Read [`docs/current-work.md`](./docs/current-work.md).
4. Read [`docs/architecture.md`](./docs/architecture.md).
5. Inspect the exact target branch/worktree and scoped diff.

GitHub PR state, deployment state and Production authorization must be verified live for the current task.

Article optimization workflow, migration evidence, production steps and rollback: [runbook](./docs/admin-layer/article-optimize-workflow.md). Current execution status remains in receipt `sanity-optimize-20260909`.

Recorded milestone (2026-09-09): PR #83 merged as `aea1f3b99bdc9408957b6d1f5dc5a75e2926fa29`; the separately approved Production normalization completed atomically (3 raw documents / 8 rows; 61-record integrity check; zero remaining patches). Web and Admin Production are READY at the same merge SHA; authenticated read-only AIA Vitality Studio visual QA passed (banner, review guard, URL locks and object cards). No article was edited or published during QA. Read the receipt for newer evidence before acting.

Recorded milestone (2026-09-10): PR #87 (`fix/content-data-foundation-20260910`) hardens Sanity reads and Blog metadata. The branch splits lightweight list/card projections from strict full-article reads, isolates invalid list records and related-article failures, ranks related articles by topic/category, adds typed/redacted Sanity error logging, and gives `/blog/` explicit Open Graph/Twitter metadata. Production Sanity documents were not edited or published; motor-insurance drafts were not touched. Neon clean views were audited as 383 rows / 383 distinct content IDs across `marketing_content_current`, `post_performance_current_clean`, and `post_performance_latest`; no SQL/schema changes were needed. Neon `main` branch protection could not be enabled because the current plan has no remaining protected-branch quota; no existing branch protection was removed.

PR #87 visual evidence (2026-09-10): authenticated Vercel Web Preview was inspected at 390 / 600 / 820 / 1100 / 1440 on Home, Blog, Health category, AIA Health CI Hero, CI Planning and Financial Health Check. Vercel Preview remains correctly isolated to Sanity UAT. Read-only Sanity counts are UAT published=0 and Production published=5, so empty Blog/category results and the representative article 404 on the Vercel Preview are expected environment behavior. Full content QA used the repository's `local:production:read` lane with Production draft writes disabled; 30 route/viewport layout comparisons had zero regression groups against current Production. Blog canonical/Open Graph/Twitter checks passed and the new related ordering for Health CI Hero is Health Happy then AIA Vitality. A small 820px CI Planning progress-label left overhang also exists on current Production and was not changed.

CSS consolidation evidence (2026-09-10): only settled FinalPolish declarations were moved into `Website43.module.css` (Thai line breaking, article source URL wrapping, Blog article-grid/category trigger, Home hero crop). `Website43TransitionStyles` and breakpoint architecture remain unchanged. Computed-style comparison against Production passed 15/15 targeted probes across all five reference widths. Local `npm run check:foundation` passed under Node 24.11.1.

PR #87 delivery state (2026-09-10): commit `6f436cbc6c80646b4b130972cc604bacb193fe1f` was pushed and Foundation CI run `34389137889` completed SUCCESS, including Production-parity build. Vercel Web deployment `dpl_7qoGpJPyrCzjJt9SP7E6RFAGtsaV` and Admin deployment `3eZJSivzbJHANNzbxGGgqQZAYezD` completed successfully. The refreshed authenticated Web Preview was rechecked across all five reference widths: Home, CI Planning and Financial Health Check remained zero-diff against Production; Blog/category/article differences remain only the expected UAT published=0 lane, and Blog social metadata still passed. Final `listArticles()` consumer review confirmed no list consumer requires full body/FAQ/sources/review/geo data. The PR remains scoped to eight files and does not mutate Sanity Production content, Neon, routing, analytics, consent or protected URLs. After this documentation-only evidence update, re-verify the resulting CI/Preview head; if green, merge PR #87 to `v4-production` and immediately perform read-only Production checks for Blog/article rendering, OG metadata, publish freshness configuration and runtime errors.

Recorded milestone (2026-09-11): branch `fix/seo-copy-source-of-truth-20260911` synchronizes the user's approved `Your Revised Text (Source of Truth)` copy into the Website code without touching `v4-production` directly. The scoped code changes cover homepage metadata/social previews, Organization/Person structured data and verified credentials, the shared Website 4.3 author card, and the Website 4.3 footer role. Vercel Web Preview deployment `dpl_6RqMcbyowD7NQq4eUmsot3bdxihW` for commit `0f45ee111a94cf903e9b2235056870d29c660af2` reached READY with no build errors. Production merge is not authorized yet; user Preview approval is required first.

Recorded scheduling milestone (2026-09-11): PR #96 contains the private-Neon article scheduler on `feature/free-plan-article-scheduling-20260911`. Read [the scheduler runbook](./docs/article-scheduling-free-plan.md) and [the validation/readback receipt](./docs/article-scheduling-validation-20260911.md). At code SHA `e91c428e9b292d869ec9c6c5b696851628660ae4`, Foundation CI `34605930980`, scheduler safety CI `34605930932` and privacy CI `34605931043` passed; Admin Preview `dpl_Hnbhf4x7bAWSJWFtNJA5MhnWWPcb` is READY. The additive queue migration was applied to the exact UAT and Production Neon lanes and verified disabled with zero queue/audit rows. Production scheduler runtime remains NOLOGIN. No Sanity article was scheduled, edited or published. Authenticated owner Preview QA, secure Production credentials, human review and activation remain separate unresolved release gates. This milestone does not authorize merging or enabling the scheduler.

Recorded Admin Control Plane repair milestone (2026-09-13): Draft PR #113 on `fix/admin-control-plane-production-20260913` repairs the incomplete Production cutover of private Admin operational state. Production Neon project `lively-bar-43618798`, branch `br-long-resonance-b3ys5xrv` currently contains the scheduler tables but the generic Audit/Research/SEO Review tables were absent and `ccpun_admin_runtime` remained NOLOGIN. The branch adds exact Production lane guards, additive operational tables/readback without revoking scheduler privileges, owner-only System Health, dashboard readiness, explicit SEO re-audit copy and a production-safe legacy backfill. Migration rehearsal is isolated on temporary branch `br-quiet-river-b3t6udt4`; all readback booleans passed, including scheduler grant preservation and Social denial. Legacy Sanity Production inventory is 19 audit logs, 1 research snapshot and 2 SEO suggestions; source records remain untouched. Initial Vercel Preview built READY with `/snt-admin/health`. No Production DB migration, LOGIN/password, Vercel environment mutation, merge, content write or publish has been performed. Final migration apply remains an explicit Neon gate after the latest PR head passes CI/Preview.

Recorded Category Registry pre-delivery milestone (2026-09-16): PR #134 and follow-up #135 were merged separately for the staged Critical Illness zero-downtime URL cutover and Production base is now `8770906`; Category Registry delivery continues on branch `feat/sanity-category-registry-20260916`, PR #136, implementation commit `e327336`. Sanity `category` is the proposed physical Blog category source of truth with `draft | active`, guarded direct `redirectTo`, slug/uniqueness/route/canonical/redirect-loop validation, per-record fail-soft parsing and a separate semantic `BLOG_TOPIC_HUBS` layer. Blog archive/category routing/canonical/dropdown/sitemap consume the same registry; Draft visibility requires both Draft Mode and the approved deployment preview lane; malformed categories cannot take `/blog` down. The Article Studio selector reads `status == 'active'`; article slug/canonical validation prevents Category ownership collisions. The merged Critical Illness redirect/canonical contracts, Health canonical overrides and all legacy redirects are preserved. Local Node 24.11.1 `npm run check:foundation` passes on top of the merged Critical Illness cutover series, including Vercel/public regressions and Admin tests (480 pass, 0 fail, 1 skipped); deterministic motor/travel registry QA and live UAT/Production migration dry-runs also pass. No live Category status/content mutation or UAT travel lifecycle mutation has been performed at this recorded point, and PR #136 has not been merged/deployed to Production yet. The guarded migration plan sets only the exact five existing Production Category documents (`personal-finance`, `life-insurance`, `health-insurance`, `investment`, `motor-insurance`) to `active` without changing `_id`, title, slug or description; UAT additionally seeds the exact Production `motor-insurance` identity/content and keeps `personal-finance-uat` plus legacy `critical-illness` Draft. Re-verify PR #136 CI/Vercel Preview before the explicit UAT apply and live motor/travel QA.


LINE Conversation Archive cutover milestone (2026-09-18): branch `feature/line-conversation-archive-20260918`, base `v4-production` SHA `6e731ef57f041d1a2e73af4264c6edc48956f5bd`. Product contract is now locked: LINE OA Manager is the only human reply surface; `admin.ccpun.com` is an owner-only Conversation Archive / Customer Timeline / Evidence surface. Production `CCPUN_LINE_OUTBOUND_ENABLED` has been set to false and a same-SHA Admin redeploy confirmed System Health `Outbound gate = ปิด`; token/media/rich-menu gates remain available for profile/media/Rich Menu operations. The branch retires the Admin reply UI and makes the reply API return 410, hard-sets Admin `outboundEnabled/replyEnabled=false`, and the new archive migration revokes `ccpun_admin_runtime` EXECUTE on enqueue/claim reply functions. The additive `20260918_line_conversation_archive_v1_*` migration adds encrypted owner-only conversation archive, encrypted LINE profile cache, owner-only LINE OA CSV reply import, evidence access audit, archive aggregate health, and a trigger that snapshots inbound encrypted message content before operational Unsend purge while retaining Unsend status/time in evidence. UAT migration applied atomically (38 statements; 2 existing messages backfilled); readback passed 17/17 booleans; synthetic Unsend transaction + ROLLBACK proved operational purge=true, evidence retention=true, archive read-only=true. Targeted security/archive tests pass 37/37 and full `npm run check:foundation` on Node 24.11.1 passes with 619 tests (618 pass, 1 skipped, 0 fail), lint 0 errors and only two pre-existing warnings. LINE profile lookup uses the server-only Messaging API profile endpoint and stores display name/picture URL encrypted; no LINE user ID is rendered client-side. Evidence View shows Unsend status, internal Archive ID and internal SHA-256 integrity fingerprint with audited Print/Save-PDF action; the fingerprint is explicitly not represented as third-party legal certification. Historical CCPun replies typed in LINE OA Manager are filled via bounded owner-only CSV import because the public Messaging API does not provide a realtime transcript feed for operator-typed OA Manager messages. Raw customer conversation access remains zero for AI/MCP/n8n AI; MCPs such as `wasintoh/line-oa-mcp-ultimate` and official `line/line-bot-mcp-server` are operator tooling only (Rich Menu/profile/quota/insight/etc.), not the archive backend. Production migration/merge/deploy/live QA remain the final delivery steps after PR checks.
